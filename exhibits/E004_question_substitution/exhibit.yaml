status: plausible_ai_generated
attribution: |
  This exhibit was generated by an AI assistant (Claude, Opus 4.6) and has not
  been validated against a real model transcript. It should be treated as a
  plausible illustration of the failure mode, not as documented empirical data.
  Requires human review and real-world reproduction before promotion to
  "observed" status.

model:
  name: "gpt-4"
  provider: "OpenAI"
  access: "API (hypothetical)"
  runtime: "OpenAI Chat Completions API"
  date_tested: "N/A — not yet tested"
  notes: >
    This exhibit has not been run against a real model. The model name is
    listed as a plausible target for reproduction. The scenario is constructed
    based on known patterns of base rate neglect in LLM reasoning.

reproducibility: not_yet_tested

failure_mode:
  primary: Question Substitution (Conditional Probability Inversion)
  secondary:
    - Base Rate Neglect
    - Epistemic Overconfidence
  category: single_turn_reasoning

description: |
  Plausible single-turn medical statistics query where a model substitutes
  sensitivity (probability of positive test given disease) for positive
  predictive value (probability of disease given positive test). Illustrates
  Kahneman's attribute substitution heuristic in LLM reasoning — when faced
  with a Bayesian inference problem, a model may default to reporting the
  easier inverse conditional. The constrained variant is hypothesized to
  produce the correct answer, suggesting a default strategy failure rather
  than a capability limitation. This exhibit is AI-generated and awaits
  real-world reproduction.
